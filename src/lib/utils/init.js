// @Flow

export function run(config?: ?Object): Object {
  console.log('Initalising the Cake App...');

  /**
   * Author note:
   * 
   * This is a simple app, but in this area we can initiate the pre 
   * requesting of app data, setting custom variables and a while host of other 
   * functions. 
   * 
   * This way we can get the loading spinners in the UI ASAP for 
   * a good UI experience and make the loading of applications seem seemless.
   */
  
  console.log('Requeting all cakes from the server');

  return {}
}